---
- hosts: evermetrics*
  sudo: yes
  pre_tasks:
    - name: 'install python2'
      raw: sudo bash -c "test -e /usr/bin/python || (apt -qqy update && apt install -qy python-minimal)"
    # - name: install python2
    #   raw: sudo apt-get -y install python-simplejson
    #   raw: add-apt-repository -y ppa:fkrull/deadsnakes-python2.7
      # - apt: update_cache=yes
      # - raw: sudo apt-get -y install python-simplejson
      # - raw: apt-get install -y python2.7 --force-yes
      # - name: Update apt repository
      #     apt: update_cache=yes
  roles:
    - evermetrics
  vars:
    - projects:
      - evermetrics-dbs
      - evermetrics-client
      - evermetrics-api
      - evermetrics-ws
      - evermetrics-front
    - services:
      - evermetrics-api-estadisticas
      - evermetrics-api-users
      - evermetrics-api-auth
      - evermetrics-ws
      - evermetrics-front
